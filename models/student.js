class Student {
  constructor(id, firstName, lastName, fatherName, classEnrolled, age) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.fatherName = fatherName;
    this.classEnrolled = classEnrolled;
    this.age = age;
  }
}

module.exports = Student;
